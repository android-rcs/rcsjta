Build instruction:

Prior to build the core package please create the third party libraries by entrering the folowing command line:

> ant libs

This will create the following jar files under the "libs" directory:
- bouncycastle.jar
- dnsjava.jar
- nist_sip.jar